web:
    image: shaneburkhart/construction-vr-demo
    ports:
        - "3000:4567"
    links:
        - redis
    volumes:
        - .:/app
    env_file:
        - user.env
    command: "rerun --background ruby server.rb"

web_prod:
    image: shaneburkhart/construction-vr-demo
    ports:
        - "4567:4567"
    links:
        - redis
    volumes:
        - .:/app
    env_file:
        - user.env
    command: "ruby server.rb"

testing_worker:
    image: shaneburkhart/construction-vr-demo
    volumes:
        - .:/app
    env_file:
        - user.env
    command: "ruby workers/skp_model_tester.rb"

redis:
    image: redis:3.2
