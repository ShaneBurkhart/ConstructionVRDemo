html
    head
        title Login
    body
        form(action="/api2/login" method="post")
            input(name="email" type="text")
            input(name="password" type="password")
            button(type="submit") Login
