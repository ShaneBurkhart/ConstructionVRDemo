extends ./layout.pug

block title
    | Dashboard -&nbsp;

block content
    script.
      var USER_ID = "#{currentUser.id}";
      var USERNAME = "#{currentUser.username}";
      var IS_SUPER_ADMIN = #{currentUser.isSuperAdmin()};

    #projects-dashboard-app
    script(src="/dist/admin_dashboard_app.js?v=04282001")