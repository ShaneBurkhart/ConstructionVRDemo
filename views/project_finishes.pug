extends layout.pug

block content
  script.
    var IS_SUPER_ADMIN = #{permissions.isSuperAdmin};
    var IS_EDITOR = #{permissions.isEditor};
    var PROJECT_ACCESS_TOKEN = "#{access_token}";
    var PROJECT_NAME = "#{projectName}";
    var HAS_RENDERINGS = "#{hasRenderings}";
    var RENDERINGS_LINK = "#{hasRenderings ? renderingsLink : ''}";
  
  #finishes-app

  script(src="/dist/finishes_app_revision.js?v=04282001")